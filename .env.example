# Copy to `.env` for local analytical work. Never commit real secrets.
#
# Used by Gate 7B PostgreSQL loader / local SQL analysis only.
# The public static dashboard does NOT require DATABASE_URL.
DATABASE_URL=postgresql://localhost:5432/ecommerce_power_shift
